<%= form_with(model: venda, local: true) do |form| %>
  <% if venda.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(venda.errors.count, "error") %> prohibited this venda from being saved:</h2>

      <ul>
        <% venda.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :codigo_venda %>
    <%= form.number_field :codigo_venda %>
  </div> 

  <div class="field">
    <%= form.label :data_venda %>
    <%= form.date_select :data_venda %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
